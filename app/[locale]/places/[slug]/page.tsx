import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { buildLocaleAlternates, BASE_URL } from "../../../../lib/alternates";
import { destinationImage, getTravelDestination, travelDestinations } from "../../../../data/travel-destinations";
import { isLocale } from "../../../../lib/blog";
import type { Locale } from "../../../../lib/i18n";
import { Breadcrumb } from "../../../components/Breadcrumb";
import styles from "../travel.module.css";

const COPY:Record<Locale,{home:string;places:string;why:string;highlights:string;plan:string;season:string;duration:string;arrival:string;tip:string;gallery:string;source:string;related:string;map:string}>={en:{home:"Home",places:"Places",why:"Why go",highlights:"What to see",plan:"Plan your visit",season:"Best time",duration:"Time needed",arrival:"Getting there",tip:"Practical tip",gallery:"See the landscape",source:"Destination facts reviewed using official Switzerland Tourism information.",related:"Continue exploring",map:"Open in Maps"},uk:{home:"Головна",places:"Місця",why:"Чому варто поїхати",highlights:"Що подивитися",plan:"Сплануйте поїздку",season:"Коли їхати",duration:"Скільки часу",arrival:"Як дістатися",tip:"Практична порада",gallery:"Подивіться краєвид",source:"Факти про місце перевірено за офіційними матеріалами Switzerland Tourism.",related:"Продовжити подорож",map:"Відкрити на мапі"},de:{home:"Startseite",places:"Orte",why:"Warum hinfahren",highlights:"Was ansehen",plan:"Besuch planen",season:"Beste Zeit",duration:"Zeitbedarf",arrival:"Anreise",tip:"Praktischer Tipp",gallery:"Landschaft ansehen",source:"Fakten mit offiziellen Informationen von Schweiz Tourismus geprüft.",related:"Weiter entdecken",map:"In Maps öffnen"}};
export function generateStaticParams(){return ["en","uk","de"].flatMap(locale=>travelDestinations.map(place=>({locale,slug:place.slug})))}
export async function generateMetadata({params}:{params:{locale:string;slug:string}}):Promise<Metadata>{if(!isLocale(params.locale))return{};const p=getTravelDestination(params.slug);if(!p)return{};const suffix=params.locale==="uk"?"що подивитися, маршрут і поради":params.locale==="de"?"Sehenswürdigkeiten, Anreise und Tipps":"things to see, route and travel tips";return{title:`${p.title[params.locale]}: ${suffix}`,description:`${p.summary[params.locale]} ${p.description[params.locale]}`.slice(0,158),keywords:[p.title[params.locale],p.region[params.locale],params.locale==="uk"?"куди поїхати у Швейцарії":"Switzerland travel guide",params.locale==="uk"?"що подивитися у Швейцарії":"places to visit Switzerland"],alternates:buildLocaleAlternates(params.locale,`/places/${p.slug}`),openGraph:{title:p.title[params.locale],description:p.summary[params.locale],url:`${BASE_URL}/${params.locale}/places/${p.slug}`,type:"article",images:[{url:destinationImage(p),width:1122,height:1402,alt:p.alt[params.locale][0]}]},twitter:{card:"summary_large_image",images:[destinationImage(p)]}}}
export default function DestinationPage({params}:{params:{locale:string;slug:string}}){if(!isLocale(params.locale))notFound();const place=getTravelDestination(params.slug);if(!place)notFound();const locale=params.locale;const c=COPY[locale];const related=travelDestinations.filter(x=>x.slug!==place.slug&&(x.category===place.category||x.region[locale].split(" · ")[0]===place.region[locale].split(" · ")[0])).slice(0,3);const schema={"@context":"https://schema.org","@type":"TouristAttraction",name:place.title[locale],description:place.description[locale],image:[0,1,2].map(i=>`${BASE_URL}${destinationImage(place,i)}`),url:`${BASE_URL}/${locale}/places/${place.slug}`,geo:{"@type":"GeoCoordinates",latitude:place.coordinates.latitude,longitude:place.coordinates.longitude},touristType:[place.category,"nature","culture"],isAccessibleForFree:true};return <main lang={locale} className={styles.page}><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,"\\u003c")}}/><article className={styles.shell}><Breadcrumb items={[{name:c.home,url:`/${locale}`},{name:c.places,url:`/${locale}/places`},{name:place.title[locale],url:`${BASE_URL}/${locale}/places/${place.slug}`}]}/><header className={styles.detailHero}><Image src={destinationImage(place)} alt={place.alt[locale][0]} fill priority sizes="100vw"/><div className={styles.shade}/><div className={styles.heroCopy}><p className={styles.eyebrow}>{place.region[locale]} · {place.category}</p><h1>{place.title[locale]}</h1><p>{place.summary[locale]}</p><div className={styles.facts}><div><span>{c.season}</span><strong>{place.season[locale]}</strong></div><div><span>{c.duration}</span><strong>{place.duration[locale]}</strong></div><div><span>{c.arrival}</span><strong>{place.arrival[locale]}</strong></div></div></div></header><div className={styles.body}><div className={styles.prose}><h2>{c.why}</h2><p>{place.description[locale]}</p><p>{place.whyVisit[locale]}</p><h2>{c.highlights}</h2><ul className={styles.highlights}>{place.highlights[locale].map(x=><li key={x}>↗ {x}</li>)}</ul></div><aside className={styles.aside}><p className={styles.eyebrow}>{c.plan}</p><h2>{place.title[locale]}</h2><dl><div><dt>{c.season}</dt><dd>{place.season[locale]}</dd></div><div><dt>{c.duration}</dt><dd>{place.duration[locale]}</dd></div><div><dt>{c.arrival}</dt><dd>{place.arrival[locale]}</dd></div><div><dt>{c.tip}</dt><dd>{place.tip[locale]}</dd></div></dl><a className={styles.mapLink} href={`https://www.google.com/maps/search/?api=1&query=${place.coordinates.latitude},${place.coordinates.longitude}`} target="_blank" rel="noreferrer noopener">{c.map} ↗</a></aside></div><section><p className={styles.eyebrow}>{c.gallery}</p><div className={styles.gallery}>{[1,2].map(i=><figure key={i}><Image src={destinationImage(place,i)} alt={place.alt[locale][i]} fill sizes="(max-width: 900px) 100vw, 60vw"/><figcaption>{place.alt[locale][i]}</figcaption></figure>)}</div></section><p className={styles.source}>{c.source} <a href={place.sourceUrl} target="_blank" rel="noreferrer noopener">Switzerland Tourism ↗</a></p>{related.length?<section className={styles.related}><h2>{c.related}</h2><div className={styles.relatedGrid}>{related.map(p=><Link key={p.slug} href={`/${locale}/places/${p.slug}`} className={styles.relatedCard}><p className={styles.eyebrow}>{p.region[locale]}</p><h3>{p.title[locale]}</h3><span>{c.related} ↗</span></Link>)}</div></section>:null}</article></main>}
